# Prompt contract: code-reviewer

Request:

Review the current branch for bugs, regressions, and structural drift.

Expected behavior:

- `code-reviewer` is the review agent
- findings come first
- findings are ordered by severity
- no fixes are implemented by the reviewer
