# Tests

Tests are grouped by product primitive, not by transport or harness.

- `tests/skills/` stores prompt contracts for skill activation and output shape.
- `tests/agents/` stores prompt contracts for custom agent use.

If you add a skill or agent, add one prompt contract here.
