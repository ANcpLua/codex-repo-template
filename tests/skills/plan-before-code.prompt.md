# Prompt contract: plan-before-code

Request:

Add a settings page for profile and notification preferences.

Expected behavior:

- `plan-before-code` activates
- the response contains current state, target state, delta, files, and acceptance criteria
- no code appears before the plan
